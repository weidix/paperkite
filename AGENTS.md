# 项目守则

本文件是本项目开发与维护的唯一行为守则，对仓库中的一切代码变更生效。

## 定位

本项目是一个以 TypeScript 编写的 Telegram 自动化运行时：触发器、动作、定时任务与常驻服务统一声明于 YAML 配置，插件只以能力名接入运行时。项目以自身身份存在，不携带任何来源信息。

## 核心思维

- **原子统一**：`triggers` / `commands` / `schedules` / `services` 四类运行原子的配置结构统一；动作统一为 `capability + session + config`，与具体插件解耦。
- **按需注入**：运行时只加载配置实际引用的能力；未被引用的插件不执行任何初始化。
- **统一控制契约**：任何前端（CLI、Web 控制台、托盘等）都通过同一份运行时控制契约取数、操作、订阅事件；热重载不重启进程、不重建会话池。
- **钩子**：动作可挂载导出函数的 TypeScript 钩子模块，在发送前转换或跳过本次载荷。
- **会话归运行时**：Telegram 会话生命周期由运行时统一持有，插件只以会话名访问。

## 核心边界

core（运行时核心）与插件、前端、数据之间的边界约束：

- **能力即契约**：core 只经能力名与参数调用插件，源码中不含任何插件实现；新增能力以注册方式接入。
- **会话归 core**：Telegram 会话的创建、连接、读写与销毁只由 core 持有；插件经受控句柄按会话名请求，同一会话的操作串行执行、互不交错。
- **调度归 core**：core 运行在单一事件循环上，负责全部原子的启动、调度与关闭顺序；插件只提供同步函数或 Promise 回调。
- **配置两分**：设置（平台参数）与运行清单（原子声明）分开存放；core 只解析自己的配置段，插件参数一律位于各自 payload 内。
- **控制面统一**：CLI、Web 控制台、托盘等前端只经 core 提供的同一契约取数、下发操作、订阅事件。
- **插件隔离**：插件互不引用、互不共享可变状态；core 只加载运行清单实际引用的能力，未引用插件保持休眠。
- **数据边界**：凭据、会话与核心数据文件只由 core 访问；插件经契约读写自己的数据，落盘于指定目录。
- **依赖收敛**：core 的第三方依赖面最小化；业务性依赖（客户端、存储驱动等）随插件分发。
- **信任与安全**：被加载的插件视为本地可信代码；core 在参数校验、能力注册、路径与凭据处理上执行保守策略，在信任与安全之间取平衡。

## 守则

1. **身份**：不保留任何来源项目的名称、备注、标识；不出现「迁移自…」一类的描述。
2. **注释**：非必要不加注释；高可读性代码优先于注释，代码即文档。
3. **结构**：不沿用任何既有工程的目录结构；先完整理解模块与逻辑，再以 Node 生态最佳实践重新设计，力求极致清晰。
4. **实现**：不搬照底层实现模型（如多线程）；一律使用 Node 生态的更优方案实现同等能力。
5. **插件系统**：插件是独立的 npm 分发单元；安装走 CLI 命令并转交 pnpm（参考 dsh 的 profile 式安装：独立 package.json + node_modules），插件代码不散落、不纠缠在项目源码树中。
6. **语言**：全部代码使用 TypeScript，禁止 JavaScript。
7. **测试**：测试高效、简洁；不为无意义内容添加测试用例。
8. **生态**：各项能力以 Node 生态对应物实现，彻底脱离 Python 生态。
9. **质量**：杜绝死代码、无意义代码、过度封装、过度保守；在信任使用者与安全之间取平衡。
10. **定位**：本项目以既有思维、架构与能力为基础重写而成；本文件不包含任何来源项目的具体内容。
11. **表达**：对话、文档、注释、提交信息等任何可见区域禁止「不是…而是…」「做……为了避免……」「做……防止……」「只做了……没有做……」「直接做……不做……」「只做……不做……」等强行对比、引用并否定上下文、为否定内容解释原因的表述；此类内容对对话与业务用户均无帮助且引入噪声，视为严重违规。

## 工程

```bash
pnpm install      # 安装依赖
pnpm dev          # 开发运行（tsx）
pnpm typecheck    # 类型检查
pnpm test         # 运行测试
pnpm build        # 构建
pnpm check        # 类型检查 + 测试
```

## 安全

- 凭据、会话文件、日志、数据库不得提交到仓库。
- 测试用例不依赖真实网络或账号连接。